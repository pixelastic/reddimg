extends /_includes/_layouts/core

block config
  //- - pageTitle = "Custom title"
  //- - pageDescription = "Custom description"
  //- see also: pageAuthor, pageImage, pageTwitter, pageUrl

block content
  #hits.pr-1
  #pagination.mt-1


  textarea#hitTemplate(hidden)
    include /_includes/_templates/hit
  textarea#emptyTemplate(hidden)
    include /_includes/_templates/empty
